from Parent import Parent

# Die Taschen in denen die Kugel eingespielt werden kann
# wird mit dieser Klasse erzeugt
class Pocket(Parent):
    # x, y, Farbe und Radius des Pockets werden definiert
    def __init__(self, x, y, color, radius):
        super().__init__(x, y)
        self.color = color
        self.radius = radius



