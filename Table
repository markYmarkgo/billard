import pygame

# Farben
GREEN = (0, 128, 0)

class Table:
    def __init__(self, left, top, width, height):
        self.left = left
        self.top = top
        self.width = width
        self.height = height

    def draw(self, screen):
        pygame.draw.rect(screen, GREEN, (self.left, self.top, self.width, self.height))
